@startuml
note across
Scenario 1 – Map Re-centering on Origin and Destination Selection
"This scenario illustrates how the map visualization is updated when
the user selects an origin and a destination. The map view is adjusted
to focus on the area that contains the selected locations."
end note

actor "User" as User
participant "User Interface" as UI
participant "API Layer" as API
participant "Data Management" as DM
participant "Map Services" as MapAPI

User -> UI : Select origin and destination

UI -> MapAPI : I_MapDataAccess: fetchMapData(area = origin/destination bounds)

MapAPI --> UI : I_MapDataAccess: MapData

par
    UI -> UI : Render map centered on origin and destination
    UI -> UI: Display Origin and Destination
end

UI -> User: Show rendered map
@enduml
